# 开发日志

## 2026-08-25

- 完成 ESP-IDF 5.4.4 环境配置及 ESP32-S3 编译、烧录、串口监视流程。
- 实现板载 RGB LED 红灯闪烁测试，并完成开发板启动验证。
- 实现 `W/S/A/D` 键盘遥控、急停和 600 ms 失联自动停车。
- 接入四路红外传感器，实现基础巡线、直角转向和丢线搜索。
- 接入左右轮编码器计数与初版双轮同步控制，并整理一键运行脚本和接线文档。

## 2026-08-26（上午）

- 完成三路电机抬轮测试及 B/C 插头调整。当前 A 通道驱动右轮，B 通道驱动左轮，C 通道由程序保持停止；同时修正了换线后的电机转向。
- 验证 A、B 两路编码器，当前分别使用 GPIO16/17 和 GPIO8/18，并将双轮速度 PI 用于补偿左右轮转速差异。
- 修正四路红外传感器映射为 OUT1→GPIO13、OUT2→GPIO14、OUT3→GPIO21、OUT4→GPIO47；OUT4 接线修复后能够正常读取亮灭变化。
- 针对直线来回抖动和过弯修正过大的问题，降低普通循迹速度及 PID 增益，加强误差滤波并限制 PI/PID 最大修正量；保留直角、锐角转弯状态机和丢线搜索机制。
- 当前普通循迹 PWM 基准为 130，范围为 105～175；转弯时外侧轮 PWM 为 170、内侧轮 PWM 为 135。

## 2026-08-26（下午）

- 最终核对三轮实际布局为 D=左前轮、A=右前轮、B=后轮，并将原 C 通道整体迁移到 D：PWMD→GPIO40、DIN1/2→GPIO42/41、E4A/B→GPIO2/1。独立诊断中 A、B、D 三轮正反转及各自编码器均通过。
- 将正常前进、后退和巡线改为 A/D 两个前轮驱动，B 后轮停止；巡线速度 PI 同步改为读取 A/D 编码器。三轮横移动作采用 A=`-90`、D=`+90`、B=`+180`，反向横移取相反极性。
- 接入 HC-SR04：集成模式使用 TRIG=GPIO48、ECHO=GPIO39（ECHO 经 5V→3.3V 分压），每 80ms 输出一次距离；增加独立 `ultrasonic_test.c` 便于排查接线和回波超时。
- 增加 `capture_telemetry.py` 和 Windows 启动脚本，能够在键盘驾驶或轮组诊断时同步保存超声、四路红外、A/B/D 编码器、瞬时速度、方向、PWM 和避障阶段。
- 根据实测日志过滤电机 PWM 引起的 20～28mm 假回波：运动中测距前短暂关闭驱动桥约 2ms，并要求连续三次距离位于 80～150mm 才触发一次性绕障。
- 实现硬编码绕障状态机：触发后停车，三轮低速向左走大半径弧线并用红外重新找线；绕障完成后，稳定红外状态由 `0000` 直接变为 `1111` 时锁定停车。
- 场地日志确认三轮弧线可以离开原线并找到下一条黑线，但三轮原地掉头时 B 后轮在 PWM 145 下持续堵转，导致编码器条件无法完成。最新版已改为 A/D 双前轮差速掉头、B 后轮释放、PWM 170，并只检查 A/D 编码器；该参数已通过编译，仍需下一轮场地实测校准 550 计数对应的实际角度。
- 下午统一使用 ESP-IDF 5.5.5 完成主程序和诊断程序的编译、烧录与串口测试；接线、运行、采集和急停流程已同步更新到 README 与 WIRING 文档。

## 2026-08-27

- 将障碍物处理暂时缩减为单步调试逻辑：小车释放后轮并使用 A/D 前轮原地对齐黑线，四路红外连续确认三次 `1001`（白、黑、黑、白）后立即停车并退出循迹。
- 暂时移除障碍后的自动左移、找新线、掉头、恢复循迹和终点判断；保留 3 秒对齐超时停车保护，便于逐步调试。
- 精简串口输出：取消高频 23 字段遥测、PID 分量和编码器明细，改为每 250ms 输出赛道四位状态、当前动作、控制状态和超声波距离。
- 横移测试加入 A/B/D 三轮编码器速度 PI：每 150ms 调整一次 PWM，使 A/D 计数保持一致、B 计数约为前轮两倍，以补偿电机和落地摩擦差异。
- 避障流程在 `1001` 对齐稳定后自动向左横移至 B 后轮约 947 计数（2⅓ 圈），再直行至 A/D 前轮各约 609 计数（1.5 圈），随后向右横移搜索黑线；任一路红外稳定看到黑线即恢复循迹，各阶段均设超时保护。
- 为避免高速右移越过黑线，将右横移找线基础 PWM 单独降低为 A/D=140、B=280，并使用对应的低速 PI 目标；右移搜索超时延长为 8 秒。
- 绕障直行距离由 1.5 圈改为 A/D 各 2 圈（约 812 计数）。重新循迹并首次稳定回到 `1001` 后开启终点检测，连续确认 `0000` 横线或任一左右转弯形态三次后停车锁定。
- 右移找线基础 PWM 再降为 A/D=120、B=240，并把右移 PI 目标速度减半。重新循迹居中到 `1001` 后新增 A/D 各约 203 计数（半圈）的延迟距离，走完后才开启终点检测。
- 根据场地测试，将绕障后的直行距离由 A/D 各 2 圈调回 1.5 圈（约 609 计数）。
- 障碍物触发后的起始定位新增 100±10mm 距离闭环：优先保持红外 `1001`，距离偏远慢速前进、偏近慢速后退；方向与距离连续稳定三次后才开始左移，联合定位超时为 6 秒。
- 根据新路线移除“定长直行后向右横移找线”，改为左移结束后用 A/D 差速向右旋转约 122 计数（按 550 计数≈180°估算约 40°，B 释放），再以 PWM 160 直行；确认经过 `1111` 后首次稳定看到黑线即恢复循迹。
- 根据场地反馈恢复“左移 2⅓ 圈 → 前进 1.5 圈 → 右移找线”的绕障路线，并将整个定位/绕障过程降速：方向对齐 PWM 100、距离调整 120、左移 A/D=150、B=300、前进 140、右移 A/D=100、B=200。
- 低速距离调整无法克服落地静摩擦，因此取消障碍前的 10cm 前后位置闭环；超声波仅负责 8–15cm 障碍触发和状态显示，触发后只完成红外 `1001` 方向对齐。
- 为排查左移结束时后轮过快造成的车头右偏，手动和自动的左右横移统一改为 A/D 前轮固定 PWM 100、B 后轮固定 PWM 200，并关闭横移速度 PI 输出。
- 根据测试要求恢复快速左移：A/D 前轮基础 PWM 150、B 后轮基础 PWM 300，并恢复横移速度 PI；右移继续使用 A/D=100、B=200。
- 取消避障后的自动右移和 1.5 圈定长前进：快速左移结束后直接以 PWM 140 直行搜索黑线，经过 `1111` 后稳定看到任一路黑线即恢复循迹，并继续原有终点检测流程。
- 将避障左移距离从 B 后轮 2⅓ 圈调整为 2¼ 圈，按 406 计数/圈计算目标约 913 计数。
- 将避障左移距离继续调整为 B 后轮 2.5 圈，目标约 1015 计数。
- 将 LQ_TFT18SPIV33 以 MODE 3 参数整合到循迹避障固件；屏幕在独立低优先级任务中显示 A/B/D 三轮实测 RPM 和 HC-SR04 距离，避免 SPI 整屏刷新阻塞循迹控制循环。
- 绕障横移后的直行找线阶段加入 B 后轮 PWM 60 的微调分量，并根据实车测试反转 B 轮极性；普通循迹和手动直行仍保持 B 后轮停止。
- 根据落地测试将直行找线阶段的 B 后轮微调 PWM 从 60 提高到 80。
- 将直行找线阶段的 B 后轮补偿限制为约一圈：从该阶段起点累计 406 个编码计数后停止 B，A/D 前轮继续直行找线。
- 根据落地测试将直行找线阶段的 B 后轮补偿距离从一圈改为 1.5 圈，目标约 609 个编码计数。
- 根据落地测试将直行找线阶段的 B 后轮补偿距离从 1.5 圈改为 2 圈，目标约 812 个编码计数。
- 将直行找线阶段的 B 后轮动作改为 PWM 80 正转 3 圈（约 1218 计数），随后反转 1 圈（约 406 计数）并停止；A/D 在两阶段持续直行找线。
- 根据落地测试将 B 后轮第一阶段正转距离从 3 圈改为 2.5 圈（约 1015 计数），随后仍反转 1 圈。
- 将 HC-SR04 避障触发距离从 8–15cm 调整为 5–8cm，仍要求连续三次有效测量后停车并进入避障流程。
- 将 HC-SR04 避障触发条件调整为任意有效距离小于等于 10cm，仍要求连续三次确认。
- 重构绕障路线为“三编码器闭环左移 → A/D 前轮各前进一圈 → 低速右移找线 → 恢复循迹”。左移不再只依据 B 后轮：A、D 计数按横移轮速比归一化后与 B 联合判断距离和姿态，累计不平衡用于 PWM 修正，末段渐进减速，并连续确认三轮编码器归零后才进入前进阶段；右移期间连续三次检测到黑线才切回循迹。
- 将三编码器闭环左移后的直行距离从 A/D 各一圈调整为各两圈（约 812 计数），B 后轮保持停止。
- 将避障左移目标从后轮等效2.5圈缩短为2圈（约812计数），继续使用A/B/D三编码器联合闭环、末段减速和停稳确认。
- 根据实车位置将避障左移目标再减少半圈，调整为后轮等效1.5圈（约609计数）；直行仍保持A/D各两圈。
- 将避障左移目标调整为后轮等效1又3/4圈，按406计数/圈四舍五入为711计数；直行仍保持A/D各两圈。
- 将避障左移由三轮平均进度判断改为独立位置闭环：A/D目标各约356计数、B目标711计数，各轮按剩余位置连续降速并叠加同步误差修正；归一化差值容差收紧至50计数，到位后进入TB6612短制动80ms，再连续确认三轮编码器静止。
- 根据车身必须保持方向的实车约束，将左移控制调整为级联闭环：恢复三轮同步速度PI及累计姿态修正，不再允许单轮提前停止；外层以三路归一化共同进度控制统一减速，差值容差收紧至35计数，并在目标附近让三轮同时短制动。
- 在保留三轮级联闭环、统一减速和同时制动的前提下，将左移共同目标从1又3/4圈改为1.5圈（609后轮等效计数）。
- 修复左移到达距离后仍持续运动的问题：原停止条件额外要求三轮归一化差值收敛至35计数，而末段最低PWM可能使该条件无法满足。现改为共同进度到达609计数即三轮同时制动，姿态差值继续用于运动中闭环修正；差值超过180计数时直接异常停车，不再靠继续左移等待收敛。
- 将修复后的左移共同目标由1.5圈调整为2圈（812后轮等效计数）；达到目标立即制动、三轮同步和异常差值保护保持不变。
- 将左移后的前进距离改为A/D各1.5圈（609计数），并加入与左移同类的双编码器级联闭环、共同减速、80ms主动制动和停稳确认。低速右移阶段也加入A/B/D归一化累计误差同步修正及异常差值停车；红外连续确认黑线后先同时制动并确认三轮停稳，再恢复循迹，减少车身歪斜和滑过黑线。
- 为避免避障后前进位置闭环在落地负载下扭矩不足，将A/D基础PWM由140提高到180、末段最低PWM由90提高到140、闭环上限提高到230；1.5圈目标及同步制动逻辑不变。
- 将三轮级联闭环左移目标从2圈调整为2又1/4圈，按406计数/圈四舍五入为914个后轮等效计数；其余前进和右移闭环参数保持不变。

## 2026-08-27～2026-08-28（USB 摄像头）

- 新建独立 ESP-IDF 工程 `camera-usb-test`。该固件只负责 USB 摄像头和 TFT，
  不初始化电机、编码器、红外、超声波，也不与 `tb6612-motor-a-test` 循迹固件
  同时运行。
- 摄像头采用 USB Host 接入：5V、GND、D-→GPIO19、D+→GPIO20；UART0 使用
  GPIO43/44、115200 baud。实测设备 VID/PID 为 `349c:3307`，产品名 `HD video`，
  Full Speed USB，能够稳定打开 640×480 MJPEG、15 fps 视频流。
- 当前摄像头的 UVC 描述符顺序和 Bulk 数据分片不符合常见实现：端点描述符早于
  VideoStreaming 类描述符，单帧数据还会跨多个 512-byte 传输。工程内保留定制的
  `usb_host_uvc` 兼容层，修正描述符归属、Bulk 长度处理和跨传输帧组装，并按 JPEG
  `FFD9` 结束标记去除尾部填充。
- 使用 ESP JPEG 将 640×480 MJPEG 按 1/4 比例解码为 160×120 RGB565，再旋转为
  120×160 写入 128×160 ST7735 TFT。TFT 接线为 SCK→GPIO3、MOSI→GPIO4、
  CS→GPIO0、D/C→GPIO38、RST→EN/RST。
- 摄像头回调使用两个 PSRAM MJPEG 缓冲槽，显示任务独立解码并丢弃来不及显示的
  旧帧，避免 USB 接收被屏幕刷新阻塞。串口 `FRAME_STATUS` 中 `received` 和
  `displayed` 已连续增长；偶发损坏 JPEG 会自动跳过，不影响后续视频。
- TFT SPI 由保守的 4 MHz 提升到 20 MHz，实测刷新率由约 3.1 fps 提升到约
  4.3 fps；同时将 ST7735 命令从开启反显改为关闭反显，修正负片式颜色。
- 修复显示任务长期占用 CPU1 导致的任务看门狗提示：每帧完成后明确让出 10 ms
  时间片。重新烧录后连续运行稳定，显示计数持续增长且无看门狗复位。
- 已将摄像头工程、接线说明、可复现配置和定制 UVC 组件提交到 GitHub；根目录
  README 明确区分 `camera-usb-test` 与原循迹/避障工程。

## 2026-08-28（任务1验收与任务2启动）

- 拉取并复核仓库最新版本 `ec9dbd7`。本轮新增内容主要来自提交 `d55d5aa`、
  `7f8b349`、`9f15ff7` 和 `ec9dbd7`，分别覆盖三轮遥测与超声避障、任务1测试版
  归档、独立 USB 摄像头/TFT 固件及对应开发记录。
- 实验任务1已完成验收。验收目标仍是红外巡线、约 5cm 障碍物触发后的麦轮横向
  绕障、重新找线与终点停车，以及液晶显示三路电机转速和超声波距离；摄像头测试
  属于后续任务准备，不是任务1的必需功能。
- 液晶先通过独立工程 `tft-display-test` 完成硬件验证：LQ_TFT18SPIV33 使用
  SCK→GPIO3、SDI/MOSI→GPIO4、CS→GPIO0、D/C→GPIO38、RST→EN/RST，
  以 128×160 ST7735S、SPI Mode 0、2MHz 显示固定文字并周期切换黑白画面。
- 液晶随后集成到 `tb6612-motor-a-test`：按实测选定的 MODE 3 参数运行，在独立
  低优先级任务中每 500ms 刷新 A（右前）、B（后轮）、D（左前）三路编码器实测
  RPM 和 HC-SR04 距离（mm）。显示刷新与 10ms 小车控制循环解耦，液晶初始化失败
  时仅记录错误，不阻断小车控制。
- 独立工程 `camera-usb-test` 已完成摄像头与屏幕联合验证：摄像头通过
  D-→GPIO19、D+→GPIO20 枚举为 `349c:3307 HD video`，稳定接收
  640×480 MJPEG、15fps；图像经 1/4 解码、旋转后写入 128×160 TFT，实测显示
  约 4.3fps。双 PSRAM 帧槽、旧帧丢弃和 JPEG 损坏帧跳过机制保证 USB 接收持续
  运行，修复任务看门狗问题后连续运行无复位。
- 摄像头固件与任务1小车固件保持为两个独立工程，不能同时烧录运行。摄像头占用
  GPIO19/20 原生 USB Host，运行时通过 UART0（GPIO43/44，115200 baud）观察日志；
  测试期间关闭 11.1V 电机电源。
- 从本条开始进入实验任务2：摄像头巡线、约 5cm 障碍物横向绕行、到终点后识别
  两个小球并分别推入指定底袋，同时继续显示三路电机转速和超声距离。任务2验收时
  按实验要求断开红外传感器电源，后续巡线判断不能依赖红外输入。
- 从今天起，新的接线、参数、代码、烧录结果、场地现象和回退原因继续按日期记录在
  本日志中；未经实机验证的内容明确标记为“待测试”，不与已验收结果混写。
- 删除未完成且未跟踪的旧草案 `usb-camera-test`，保留已验证的 `camera-usb-test`
  作为摄像头/TFT基线，并从基线新建独立工程 `camera-line-follow-test`。新工程复用
  已验证的定制 UVC 组件，避免影响原任务1固件和摄像头基线。
- 完成摄像头巡线第一版（待实机测试）：640×480 MJPEG解码为160×120后，仅分析
  画面上方 `x=6..153, y=5..79`，忽略下方可能出现的车身；用自适应亮度阈值和
  最大连续暗色区域提取黑线，在近、远两个观察带计算中心并生成转向误差。
- TFT在巡线期间继续显示实时摄像头画面，并叠加总ROI、近/远观察带、画面目标中心
  和识别到的黑线中心，便于现场调整镜头方向、ROI和阈值。初版控制使用A/D前轮
  低速差速、B后轮停止；上电保持STBY低，UART收到`F`且当前黑线识别可信时才启动，
  `X`/空格急停，图像超过600ms未更新或黑线置信度不足时立即停车。
- `camera-line-follow-test` 已使用 ESP-IDF 5.4.4 完整编译通过，固件大小约
  `0x4f310`（324KB），1MB应用分区剩余约69%。当前尚未烧录；转向正负、ROI位置、
  阈值稳定性和PWM参数均标记为待架空及场地实测。
- 尝试通过板载 UART 烧录时，Windows 仅枚举出 `COM3`～`COM6` 四个蓝牙虚拟串口，
  未发现 CH340、CP210x 或 ESP32 串口设备，USB 设备层也没有新设备。因此本次未执行
  写入；需更换可传输数据的 USB 线或电脑 USB 口，确认 UART 出现新 COM 号后再烧录。

## 2026-08-28（camera idiot test hmx / 六点拟合版本）

- 从 `origin/main` 新建独立分支 `camera-idiot-test-hmx` 保存本轮六点拟合版本，不覆盖
  原 `main` 分支和工作区中的其他巡线实验版本。
- 黑线检测改为固定 RGB 黑色门限（默认 `R/G/B<=105`）、形态学去噪和连通域选择。
  有效 ROI 为 80x60 解码图中的 `x=3..76, y=12..57`；中心区域和纵向覆盖获得更高
  评分，与主体连通的边缘像素一并保留，近端落到边缘时置信度直接衰减到 0。
- 在靠近车辆的 `y=13..24` 范围内划分六个紧密采样带，提取所选连通域中心并进行
  最小二乘直线拟合；控制误差采用近端横向偏移 65% + 局部方向偏差 35%，减少直线
  过度修正和弯道切角。
- 摄像头优先尝试低分辨率 YUYV，实测设备不支持后回退到 640x480 MJPEG、15fps；
  JPEG 按 1/8 直接解码为 80x60。关闭 TFT 动态刷新后，实测视觉处理从约 3--4fps
  提升到约 13fps，串口 `FRAME_STATUS` 增加 `processed` 计数。
- A/D 前轮继续差速驱动、B 后轮停止。10 位 PWM 参数提升为：正常基础值 280、
  低置信度 220、单轮上限 420、内轮下限 70；最大修正 75、每帧最大变化 5。
  上电保持安全停车，只有新鲜画面中 `confidence>=30` 时才接受 `F`，`X` 或空格急停。
- 使用 ESP-IDF 5.5.5 完整编译并烧入 COM6。启动后摄像头稳定进入
  `STREAMING_640x480_15FPS_MJPEG`，测试画面黑线置信度约 70--77，视觉处理持续运行
  且未出现任务看门狗复位。当前 TFT 为静态状态页，屏幕不显示实时摄像头画面。

## 2026-08-31（camera-fool-test-zzh / 摄像头大弯版本）

- 从远端 `camera-idiot-test-hmx` 最新提交 `e3541c4` 新建独立分支
  `camera-fool-test-zzh`，并在独立工作树开发，不改动原工作区和其他摄像头巡线分支。
- 图像处理由近场六点直线拟合改为黑线连通域中心路径追踪：只使用 3x3 闭运算保留
  远处窄线，从近端锚点进行 8 邻域路径搜索，沿路径均匀生成最多 14 个采样点。
- 增加两段路径拟合与大弯检测。检测量是行进方向变化角：赛道内角 100 度对应约
  80 度方向变化，直角对应 90 度；当前候选阈值设为 45 度，并要求两段拟合残差相对
  单线拟合至少改善 25%、转弯置信度至少 50、同方向连续 3 帧确认。
- 新增 `TRACK -> APPROACH -> PIVOT -> REACQUIRE` 状态机。确认大弯后以 PWM 130
  接近；拐点到达 `corner_y<=31` 后，内轮 PWM 165 反转、外轮 PWM 210 正转；随后
  以两轮 PWM 145 继续原方向旋转，连续 3 帧重新对准新线后恢复普通巡线。
- 右转时 A（右前内轮）反转、D（左前外轮）正转；左转时 D 反转、A 正转；B 后轮
  始终释放。保留 `STEERING_SIGN=-1` 的现有实车方向标定。
- TFT 恢复低频摄像头预览并叠加选中连通域、完整中心路径、入弯/出弯线段和拐点。
  `FOLLOW_STATUS` 新增控制状态、大弯标志、方向、角度、置信度与拐点坐标，便于场地
  标定和排查误识别。
- 图像超时 600ms、普通巡线丢线、接近阶段丢失无有效拐点或重新捕获超时均明确
  停车；大弯强转最长 1200ms，重获黑线最长 1000ms，防止持续原地旋转。
- 使用 ESP-IDF 5.4.4 完整构建通过，固件大小 `0x59660`（约 366KB），1MB 应用分区
  剩余约 65%。本条只验证了编译，尚未烧录或进行架空/落地实车测试；方向、触发位置、
  转弯 PWM 和超时参数均需按 README 的顺序标定。
- 通过板载 CP210x UART 的 `COM9` 完成烧录，Bootloader、应用和分区表均写入并通过
  SHA 校验。启动确认 ESP-IDF 5.4.4、16MB PSRAM、TFT 20MHz 和电机安全停车正常；
  `enabled=0`，A/D 两轮 PWM 均为 0。
- 本次摄像头接线后仍未完成 USB 枚举：复位后持续停在
  `CAMERA_STATUS=WAITING_FOR_USB_CAMERA`，没有 VID/PID、视频流或有效图像帧，故尚未
  进行视觉识别和车轮测试。优先检查摄像头 5V 实测电压及 D-/D+ 接线，不将此现象
  归因于大弯算法。
- 调整摄像头供电/接线后重新测试，USB 枚举恢复并进入
  `STREAMING_640x480_15FPS_MJPEG`。约 10 秒内累计接收 110 帧、处理 84 帧、TFT
  显示 14 帧，最新帧龄约 88--156ms；偶发 JPEG 损坏帧可自动跳过，视频流持续运行。
  当前画面尚未识别到有效黑线（`line=0`），下一步先做静态直线和左右大弯图样测试，
  暂不启动电机。
- 黑线识别恢复后完成第一次落地启动。`F` 被正常接收，但初始画面连续判定为大弯，
  控制器约 0.8 秒内由 `APPROACH` 进入 `PIVOT`，随后重捕线超时自动停车并清除使能；
  因而超时后的 `enabled=0`、`wheel_a=0`、`wheel_d=0` 是安全停车结果，不是串口失效。
- 针对首次日志中接近阶段出现 `wheel_a=150`、`wheel_d=90`，后者低于旧红外巡线
  使用的有效最小 PWM 140，重新设定低速参数（待实车验证）：普通巡线基准 160，
  A/D 最低 140、最高 190、最大差速修正 20；大弯采用旧巡线参数，外轮正转 170、
  内轮反转 135，重捕线旋转 140。这样降低大弯速度，同时避免直行侧轮因 PWM 过低
  克服不了静摩擦。
- 根据摄像头安装较高且向前倾斜的几何关系，将大弯强转触发点从 `corner_y<=31`
  后移到近车端 `1/5` 区域。视觉 ROI 原始坐标为 `y=12..57` 且数值越大越远，故首轮
  试验采用 `corner_y<=21`；远处拐点丢失或 `APPROACH` 超时不再直接触发强转，而是
  回到 `TRACK` 重新判断，避免拐点仍远时提前原地旋转。该阈值及降速参数待落地验证。
- 为便于场地调试，增加明确的视觉触发标记：TFT 用青线显示与控制器共用的
  `corner_y=21` 阈值，用蓝色大十字显示当前拐点，蓝色标记到达青线才进入 `PIVOT`。
  测试模式暂时取消识别丢线停车：`TRACK/APPROACH` 保持最后一次限幅输出，
  `REACQUIRE` 不再因超时停止；摄像头帧超时、上电安全停车和 `X`/空格急停仍保留。
- 精简串口日志：状态切换和丢线/复线事件即时输出，且丢线状态连续 3 个新画面确认
  后才报告；周期 `FOLLOW` 从 500ms 降为 2s，保留状态、拐点、角度和 A/D PWM，
  `FRAME` 从 1s 降为 3s，并只保留吞吐、丢帧和帧龄。以上改动尚未构建和实车验证。
- 根据落地日志定位到转弯未触发的直接原因：拐点已从 `corner_y=33` 移到 `18`，实际
  越过了旧触发线 `21`，但该帧的瞬时视觉方向从已确认的 `-1` 翻成 `1`，旧条件因
  要求方向继续一致而拒绝转弯，随后 `APPROACH` 超时。现改为首次连续确认后锁定物理
  方向，后续只使用拐点位置触发，不再让瞬时方向翻转覆盖锁定结果。
- 将 TFT 与控制器共用的触发线提高到 `corner_y=30`，并把大弯流程改为
  `TRACK -> APPROACH -> ADVANCE -> PIVOT -> REACQUIRE`。拐点触线后 A/D 以 PWM 150
  继续前进各 203 个编码器计数（约半圈），这一段不依赖后续视觉；若 3 秒未完成，
  输出 A/D 实际计数后继续进入旋转，防止编码器异常导致无限直行。
- 强转及重捕阶段改为 A/B/D 三轮以 PWM 130 原地旋转，方向沿用已锁定的大弯方向；
  `FOLLOW` 日志新增 B 轮有符号 PWM 和 A/D 前进计数。三轮方向、PWM 130、半圈 203
  计数均为待架空及落地标定的首轮值，本轮按要求仅修改代码和文档，尚未构建或烧录。
- 根据下一轮试验要求再次降速：普通巡线基础/低置信度 PWM 调为 150/140，接近阶段
  PWM 调为 140；大弯仍必须同方向连续 3 帧确认后才能锁定。视觉触发线恢复为
  `corner_y=21`，触发后的定距直行由约半圈扩大到 609 计数（约 1.5 圈），`ADVANCE`、
  三轮 `PIVOT` 和 `REACQUIRE` 的 PWM 均降为 80。使用 ESP-IDF 5.4.4 关闭 ccache
  后完成全量构建，固件大小 `0x5a220`，1MB 应用分区剩余约 65%；尚未烧录和实车标定。
- 根据后续参数调整，将 `ADVANCE`、三轮 `PIVOT` 和 `REACQUIRE` 的 PWM 从 80 提高
  到 110；定距直行由最初半圈方案的 3 倍进一步改为 5 倍，即 A/D 各 1015 计数
  （约 2.5 圈），编码器故障保护超时相应从 3 秒放宽到 10 秒。触发线仍为
  `corner_y=21`，同方向连续 3 帧确认条件不变。ESP-IDF
  5.4.4 全量构建通过，固件大小仍为 `0x5a220`，尚未烧录和实车标定。
- 根据实车调参，将 `ADVANCE` PWM 从 110 提高到 130，定距目标由最初半圈方案的
  5 倍提高到 8 倍，即 A/D 各 1624 计数（约 4 圈）；三轮 `PIVOT` 继续使用 PWM 110，
  `REACQUIRE` 调为 PWM 105。接近 PWM 140、触发线 `corner_y=21`、10 秒前进超时、
  250--1200ms 强转时限及连续 3 帧确认条件保持不变；最新参数尚未重新构建和烧录。
- 新增独立 `camera_line_follow_simple.c` 并将当前构建切换到该文件，原
  `camera_line_follow.c` 大拐点/定距前进算法完整保留以便回退。新控制器仅在黑线有效时
  使用摄像头 `steering_error` 闭环直行；连续 3 帧丢线后按 `LEFT_90 -> RIGHT_180`
  搜索，任一阶段连续 3 帧复线即回到闭环直行，右转结束仍无黑线则关闭使能并停车。
- 左 90 度和右 180 度暂按 A/D 编码器平均 275/550 计数判断，三轮搜索 PWM 为 110，
  超时分别为 5/9 秒；转向切换时停车一个 20ms 控制周期。ESP-IDF 5.4.4 全量构建通过，
  固件大小 `0x59da0`，1MB 应用分区剩余约 65%；角度计数和三轮方向尚待实车标定。
- 为避免旋转中短暂看到黑线却在连续 3 帧确认前扫过目标，搜索阶段第一帧复线即暂停
  三轮并原地确认；连续 3 帧成立后恢复闭环直行，后续帧重新丢线则继续原方向搜索。
- 在当前简化控制器上试验横带占比巡线：保留 RGB 门限、形态学、轨迹连通域选择、
  中心路径和大弯诊断逻辑，只统计 80x60 画面 `y=12..23`（高度 1/5--2/5）横带内
  已选轨迹像素。以距中心加权的左右占比生成转向误差，偏向哪侧即向哪侧转，偏离
  越远差速越大；有效像素不足 3 个时回退原近段路径误差。`TRACK` 最大差速修正由
  20 提高到 30，并改成误差到差速的直接比例映射；丢线后的左 90°、右 180°搜索
  状态机保持不变。TFT 增加灰色横带边界，`FOLLOW` 增加左右占比、像素数和加权误差。
  ESP-IDF 5.4.4 增量构建通过，固件大小 `0x59f90`，尚未烧录和实车方向验证。
  ESP-IDF 5.4.4 增量构建通过，固件大小 `0x59fd0`，1MB 应用分区剩余约 65%；尚未
  烧录，90/180 度编码器计数和三轮旋转方向仍待实车标定。
- 按最新试验策略取消当前简化控制器的三帧大弯/复线确认、触发线、编码器定距前进、
  左 90°/右 180°搜索和三轮原地旋转。有效轨迹存在时继续使用 1/5--2/5 横带加权
  占比做 A/D 连续差速；普通识别丢线后保持上一组 A/D 指令不变，第一帧复线即恢复
  `TRACK`。摄像头帧超过 600ms 未更新以及 `X`/空格急停仍会停车，B 后轮始终停止。
  TFT 同步移除旧 `corner_y` 青色触发线，只保留灰色横带边界。ESP-IDF 5.4.4 关闭
  ccache 后全量构建通过，固件大小 `0x591f0`，1MB 应用分区剩余 65%；尚未烧录和
  实车验证。
- 按实车调试要求提高视觉有效分辨率：摄像头仍采集 640x480 MJPEG，JPEG 解码由
  1/8 的 80x60 提升为 1/4 的 160x120，旋转后逐像素完整显示在 128x160 TFT；ROI、
  横带和连通域尺度阈值同步按两倍坐标调整，横带为 `y=24..47`。转向反馈改为线性
  系数 38 加边缘二次系数 22，最大修正 60，单轮上限由 170 放宽到 190、每帧修正
  步长由 5 提高到 8，使画面边缘黑线产生更强差速而中心区域仍连续。ESP-IDF 5.4.4
  关闭 ccache 后全量构建通过，固件大小 `0x59270`，1MB 应用分区剩余 65%；尚未
  烧录，需实测 4 倍像素处理量下的 `processed` 帧率和 `age` 帧龄。
- 定位并修复 Windows 构建反复卡住问题：ESP-IDF 5.4.4 生成的 PowerShell profile
  原先硬编码 `IDF_CCACHE_ENABLE=1`，ccache 进程会长时间挂起。现已将本机 profile
  改为 0 并保留 `.bak-before-ccache-disable` 备份，同时在工程
  `.vscode/settings.json` 设置 `idf.enableCCache=false` 和环境变量 0。标准
  `idf.py -B build-local-5.4.4-user build` 全量构建成功，随后的增量构建约 9 秒完成，
  不再触发重复 CMake 配置或 ccache。
- 梳理摄像头巡线工程：默认构建从仅含 `TRACK/HOLD` 的
  `camera_line_follow_simple.c` 恢复为完整 `camera_line_follow.c`；简化实现保留为
  对照实验，但不参与默认固件。README 按入口、视觉、控制、显示和测试重新说明，
  避免修改未参与构建的控制器。
- 恢复动力足够的速度组：普通巡线基础/低置信度 PWM 160/150，接近 PWM 160，
  ADVANCE PWM 150，三轮 PIVOT/REACQUIRE PWM 130；ADVANCE 以最初 203 计数半圈为
  基准扩大到 8 倍，即 A/D 各 1624 计数（约 4 圈），超时保持 10 秒。
- 修正 160x120 升分辨率后的坐标不一致：旧 80x60 的 `corner_y=21` 等比例改为 42；
  ROI 从 y=24 开始，继续使用 21 将永远不能触发。TFT 恢复显示同源青色触发线。
- 拐点检测从“全路径二段拟合残差最小”改为沿近端到远端逐个检查局部方向变化，
  选第一个可信拐点，避免同屏两个弯时锁定远弯。路径采样由 14 增至 20 点；控制层
  除同方向连续 3 帧外增加坐标连续性检查，确认后拒绝突然变远的拐点。已确认近弯在
  触发线后方 18 像素范围内短暂丢失 300ms 时，使用锁定方向进入 ADVANCE；远处丢失
  仍按接近超时回 TRACK。ESP-IDF 5.4.4 增量构建通过，固件大小 `0x5a4d0`，1MB
  应用分区剩余约 65%；尚未烧录和实车验证。
- 根据实车日志修复重捕无法退出：`REACQUIRE` 中长期 `line=1`，且曾出现横带误差
  -45、-125 的近似对准画面，但旧条件要求 `big_turn=false`；弯道画面持续被标成
  `big_turn`，导致稳定帧数始终清零并保持三轮旋转。新逻辑改为有效黑线且实际
  `steering_error` 绝对值不超过 250 即成为重捕候选，第一帧候选立即停车，连续 3 帧
  后恢复 TRACK；候选丢失才恢复旋转。三轮 PIVOT/REACQUIRE PWM 从 130 降到 105，
  周期日志新增 `steer` 和 `stable`，便于确认退出条件。ESP-IDF 5.4.4 全量构建通过，
  固件大小 `0x5a600`，1MB 应用分区剩余约 65%；尚未烧录和实车验证。
- 同一份日志显示 ADVANCE 由 10 秒超时结束，而非达到 1624 目标：结束时 A/D 仅为
  554/1263，说明 A/D 编码器计数或轮速存在明显差异。本轮保持超时和定距条件不变，
  避免同时改变已接近合适的转弯时机；后续需单独架空检查 A 编码器 GPIO16/17。
- 根据直线实测提高转向响应速度：误差一阶滤波由“75% 历史值 + 25% 当前帧”改为
  “25% 历史值 + 75% 当前帧”。ROI `x=6..153` 左右对称，软件几何中心约为 x=80；
  车辆需要偏在线右侧才能平行直行，更可能来自摄像头安装偏航、镜头/车体运动中心
  不重合或 A/D 轮同 PWM 推力不一致。新增统一的
  `STEERING_TARGET_X_OFFSET_PX` 标定项，默认 0；横带误差、路径回退误差和 TFT 红色
  目标线共用该位置，后续结合直行时 `band_err` 与 A/D 输出校准。
- 当日最终参数将三轮 `PIVOT`/`REACQUIRE` PWM 继续由 105 降至 95，最短/最长强转
  时间保持 250/1200ms。ESP-IDF 5.4.4 增量构建通过，固件大小 `0x5a610`，1MB
  应用分区剩余约 65%；本轮仅完成构建，尚未烧录该最终版本。
- 下一轮待验证：实车在收到停车指令后仍会因机械惯性继续转过一个角度，可能扫过刚
  识别到的新线；需要评估提前停车、短时反向制动或降低重捕 PWM。普通巡线和重捕
  当前仍使用画面高度 `1/5--2/5`（160x120 下 `y=24..47`）横带，扩展到
  `1/5--3/5` 的方案尚未实施。另需继续排查 ADVANCE 阶段 A/D 编码器计数差异。
